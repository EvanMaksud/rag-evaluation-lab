# Vision Dataset Quality

Computer-vision model quality depends on annotation consistency, balanced classes, valid bounding boxes, and careful review of edge cases.

Dataset audits can reveal missing labels, duplicate images, class imbalance, and boxes that fall outside the expected image frame.

